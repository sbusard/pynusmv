def xml_representation(fsm, tlacenode, spec):
	"""
	Returns a string containing the XML representation of a TLACE
	starting at	tlacenode, explaining why the state of tlacenode,
	belonging to fsm, violates spec.
	"""
	pass # TODO